• More OpenAI models in the picker: choose GPT-5.6 Sol, Terra or Luna, or GPT-6 Sol or Luna from the built-in model list. Opening the picker also pulls the current GPT lineup from OpenAI with your saved key, so new models show up without an app update; offline or without a key it uses the built-in list
• Day-type colors across the week strip and Progress charts: each day-type profile can pin a color from an 8-swatch preset. The hero chip and the week strip show it, untracked days get a hollow marker on a muted tile, and every Progress chart gains a marker lane with per-day dots, week buckets past 90 days and dashes for untracked days. The web app round-trips the color, and entries without one keep the stable auto color
• Saving is instant, Health Connect catches up in the background: manual saves, saved-meal relogs and recipe logs write the diary first and mirror to Health Connect behind the scenes, so the sheet dismisses right away. Home shows a snackbar once per session while queued writes are still retrying. Planned meals no longer queue Health Connect retries at all
• A named weigh-as-you-go meal keeps the last photo as its thumbnail: the diary and Saved Meals show the finished plate instead of the first ingredient shot. Unnamed drafts keep per-item photos
• API key fields ask you to paste: placeholders that mimicked a filled key are gone. The onboarding key step, the Settings AI key sheets and the speech key sheet now show "Paste your API key"
• Untracked and import sheets match the other review sheets: they share the same toolbar and save bar, and queue thumbnails show a loading state instead of the missing-photo icon while a photo decodes
• Haptics on wheels, Save, Undo and plan placement: wheel pickers tick as a value settles, and Save, the Undo actions and placing a meal on a future day give a short buzz
• Serving edits settle on Save instead of every keystroke: typing an amount keeps the wheel and the text in sync while calories and macros wait, so intermediate digits no longer rescale the row. The result applies when you save, leave the field or switch the unit
• Open Food Facts requests stop at 8 seconds: searches and lookups could hang for tens of seconds on a slow connection. Every request now gives up at 8 seconds and search moves to its next attempt
• A language chosen in system Settings survives a restart: with the in-app language left on System, a cold start could wipe the per-app language picked in the system settings
• The calorie wheel covers kJ amounts: the wheel clamped the display-unit value at 5000, which kJ users hit around 1200 kcal of food. It now spans the converted equivalent, and kcal users are unchanged
• TalkBack reads inspected chart values: inspecting a point on a Progress chart announces the metric, date and value, and taps past the last bar's right edge no longer select it
• Caffeine totals follow the system locale, and the tracker card uses its own color
• Coach voice failures show up in chat: a denied microphone permission or a failed transcription now surfaces in the chat error row instead of disappearing, and the image-only send prompt is translated
• Review photos use less memory: photos in the review sheet decode size-bounded with rotation applied, like the home pipeline, instead of loading a full-size bitmap
• The plan week window shows loading and retry: it used to paint seven empty day sections until the data arrived, and nothing at all when the read failed. A spinner shows while loading, and a Retry row appears on failure
• Small controls meet the 48 dp touch target: steppers, pickers, sheet icons, the log-now check and the Progress customize chip are easier to tap and announce themselves. The onboarding provider controls gained selection semantics, and the API-key test shows a checking indicator while it runs
• Review-sheet polish: the serving hint returns under the unlocked serving card, the empty-diary text no longer flashes before the first load, and body measurements ask for a height when derived numbers are missing
• Fat reads as a floor on Home: passing the fat target shows a calm "Minimum met" instead of the over wording, and the card no longer takes the over color
• Day-type colors are translated everywhere: the color names and the week-strip accessibility label ship complete in German, Spanish, French and Polish
